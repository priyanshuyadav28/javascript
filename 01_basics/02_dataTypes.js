"use strict"; // treat all js code as newer version of js 

// alert(3 + 3) // error-> because we are using node.js not browser

let naam = "Priyanshu Yadav"
let age = 21
let isLoggedIn = true

// Data Types 

/*
    number 
    bigint => 10n
    string => ""
    boolean => true/false
    null => acts as a standalone value
    undefined => something which is undefined that means we have not defined anything to it
    symbol => unique
 */

console.log(typeof(age));

console.log("Type of null is: ", typeof(null)); // object 
console.log("Type of undefined is: ", typeof(undefined)); // undefined 


// console.table({naam, age})

